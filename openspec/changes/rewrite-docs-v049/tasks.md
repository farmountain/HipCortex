## 1. Main GitHub Repository README Rewrite

- [x] 1.1 Rewrite root `README.md` to establish HipCortex `v0.4.9` as the Autonomous Cognitive OS & Persistent Causal Substrate, featuring exact `cl100k_base` token savings tables (`Headroom Mode Top-5 at -59% to -84%`, `Caveman Mode Top-3 at -70% to -88%`) and the 6-layer architecture diagram.
- [x] 1.2 Add the **Caveman Comparison Matrix** (`0.48–0.61 ms local Rust vs 15–35 ms local Python vector vs 142 ms cloud API`) into `README.md` and `BENCHMARK.md` to ensure bulletproof, fact-checked engineering credibility without marketing over-claims.

## 2. VS Code Extension & Antigravity IDE Marketplace Documentation

- [x] 2.1 Update `vscode-extension/package.json` (`displayName`, `description`, `keywords`) and bump/verify `v0.4.9` metadata for Open VSIX Registry and VS Code Marketplace visibility.
- [x] 2.2 Rewrite `vscode-extension/README.md` with explicit zero-config instructions for Antigravity IDE and VS Code (`webserver.exe` background execution), interactive `@hipcortex` chat commands (`health`, `add`, `query`, `status`), and language model tools (`hipcortex_search`, `hipcortex_health`, `hipcortex_predict`).

## 3. Universal MCP & Multi-Agent Ecosystem Setup Guide

- [x] 3.1 Rewrite `sdk/mcp/README.md` into the comprehensive **Universal MCP & Multi-Agent Setup Guide**, providing exact JSON configuration snippets (`mcpServers`) and CLI onboarding instructions (`hipcortex setup`) across Claude Code, Cursor, Windsurf, Grok Code, Hermes, OpenClaw, Cline, RooCode, OpenAI Codex CLI, Aider, Gemini CLI, and Amazon Q Developer.

## 4. Python and TypeScript SDK Documentation Parity

- [x] 4.1 Update `sdk/python/README.md` with multi-tier memory ingestion (`WorkingSet`, `ShortTerm`, `LongTerm`, `Causal`, `Procedural`), self-model capability gates (`can_execute`), and proactive CodeAct loop snippets.
- [x] 4.2 Update `sdk/typescript/README.md` with matching TypeScript/Node SDK snippets and multi-tier memory examples.

## 5. Verification, Git Commit, and Multi-Channel Dissemination Guide

- [x] 5.1 Verify markdown formatting across all rewritten README files (`README.md`, `vscode-extension/README.md`, `sdk/mcp/README.md`, `sdk/python/README.md`, `sdk/typescript/README.md`).
- [x] 5.2 Commit all documentation updates cleanly to git (`git add -A && git commit -m "docs: rewrite v0.4.9 cognitive os documentation across repo, vscode marketplace, mcp servers, and sdks"` and `git push origin main`).
- [x] 5.3 Output the exact multi-channel dissemination and publishing checklist (`npm publish`, `vsce publish`, `pypi`) for the user.
