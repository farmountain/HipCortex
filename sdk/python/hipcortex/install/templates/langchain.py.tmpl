"""HipCortex + LangChain — drop-in memory integration.
Generated by: hipcortex install
Docs: https://github.com/farmountain/HipCortex
"""
from hipcortex.langchain_memory import HipCortexMemory

# Package-first: URL + actor from config (HIPCORTEX_URL / .hipcortex/config.toml)
memory = HipCortexMemory.from_settings()

# Explicit override (install-time server URL):
# memory = HipCortexMemory(session_id="my-agent", url="{{SERVER_URL}}")

# In a LangChain chain:
# from langchain.chains import ConversationChain
# from langchain.chat_models import ChatOpenAI
# chain = ConversationChain(llm=ChatOpenAI(), memory=memory)
# chain.predict(input="Hello!")
