{
  "name": "hipcortex",
  "version": "0.2.0",
  "description": "Persistent causal memory for AI agents — LangChain, LlamaIndex, AutoGen, Vercel AI SDK",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["ai", "memory", "langchain", "autogen", "llm", "agents", "rag"],
  "license": "Apache-2.0",
  "repository": { "type": "git", "url": "https://github.com/farmountain/HipCortex.git" },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@types/node": "^20.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  }
}
